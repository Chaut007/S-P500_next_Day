# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง (.env ถูก git-ignore ไว้แล้ว)

# yfinance ไม่ต้องใช้ key — ตัวแปรด้านล่างสำหรับกรณีเปลี่ยนไปใช้ vendor อื่น
ALPHA_VANTAGE_API_KEY=
POLYGON_API_KEY=

# ระดับ log: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
